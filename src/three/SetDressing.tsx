/* =====================================================================
   Set dressing around the room's edges: a plant, framed prints, a
   corkboard, headphones on a hook, a radiator — the things a person who
   lives here would have. Owned by the room builder; mounted once from
   Scene.tsx. The desk is the hero, so dressing stays at the edges.
   ===================================================================== */

export default function SetDressing() {
  return null
}
